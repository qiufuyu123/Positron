{
  "name": "positron",
  "version": "1.0.0",
  "description": "Windows-only injection framework for Electron programs. Attaches to a running target, captures the V8 isolate, and lets you run arbitrary JS or hook native exports — without depending on the target loading a NAPI native addon.",
  "main": "index.js",
  "directories": {
    "doc": "docs",
    "example": "examples",
    "test": "tests"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "terser": "^5.47.1"
  }
}
